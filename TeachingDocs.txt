 const hour = new Date().getHours(); // To be the logic in the Footer 
 const openHour = 12;
 const closeHour = 22

 if (hour >= openHour && hour <= closeHour ){
    alert ('We're currently open!)
    else{
        alert ('Sorry, we're closed!')
    }
 }

new Date().toLocaleTimeString()  // To get the current time in the footer